[project]
name = "lorekeep"
version = "0.8.3"
description = "Temporal knowledge graph for AI agents via MCP"
readme = "README.md"
license = {text = "MIT"}
authors = [{name = "Manh Pham"}]
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.6",
  "pyyaml>=6.0",
  "mistune>=3.0",
  "litellm>=1.40",
  "typer>=0.12",
  "rich>=13.7",
  "networkx>=3.2",
  "mcp>=1.0",
  "platformdirs>=4.0",
]

[project.scripts]
lorekeep = "lorekeep.cli:app"

[project.urls]
Homepage = "https://github.com/manhhailua/lorekeep"
Repository = "https://github.com/manhhailua/lorekeep"
Security = "https://github.com/manhhailua/lorekeep/security/policy"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lorekeep"]

[dependency-groups]
dev = ["pytest>=8.0", "pytest-cov>=5.0", "pre-commit>=4.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
